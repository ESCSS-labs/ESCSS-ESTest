import { _Common } from "./extends/_Common";

export class _Boolean extends _Common {
  constructor(...args) {
    super(...args);
    globalThis.__ESCSS_ESTEST__.analysis._Boolean._count += 1;
  }
}
